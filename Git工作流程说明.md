# Git 工作流程说明

## 📝 基本概念

### Git 的三个层次

1. **工作区**（Working Directory）- 你正在编辑的文件
2. **暂存区**（Staging Area）- `git add` 后的文件
3. **仓库**（Repository）- `git commit` 后的文件

### 推送到 GitHub 的步骤

```bash
# 1. 查看修改了哪些文件
git status

# 2. 添加修改的文件到暂存区
git add .

# 3. 提交修改（保存到本地仓库）
git commit -m "修复验证问题"

# 4. 推送到 GitHub（上传到远程仓库）
git push origin main
```

---

## 🔄 自动同步方案

### 方案 1：GitHub Desktop（推荐新手）

**优点**：
- 图形界面，简单直观
- 可以设置自动提交和推送
- 不需要记住命令

**设置步骤**：
1. 下载 GitHub Desktop：https://desktop.github.com
2. 登录你的 GitHub 账号
3. 打开你的项目
4. 在设置中可以启用自动提交

**使用**：
- 修改文件后，GitHub Desktop 会显示修改
- 点击 "Commit" 提交
- 点击 "Push origin" 推送

### 方案 2：VS Code / Cursor 的 Git 集成

**优点**：
- 在编辑器中直接操作
- 可视化界面
- 不需要切换窗口

**使用步骤**：
1. 在 VS Code/Cursor 中打开项目
2. 左侧边栏点击 "Source Control"（源代码管理）图标
3. 修改的文件会显示在列表中
4. 点击 "+" 添加到暂存区
5. 输入提交信息，点击 "✓ Commit"
6. 点击 "..." → "Push" 推送到 GitHub

### 方案 3：Git Hooks（自动提交，需谨慎）

**警告**：自动提交可能导致提交过多或提交错误代码

如果你确实想要自动提交，可以设置 Git Hook：

```bash
# 创建 pre-commit hook（提交前自动添加所有文件）
cat > .git/hooks/pre-commit << 'EOF'
#!/bin/sh
git add -A
EOF

chmod +x .git/hooks/pre-commit
```

**不推荐**：因为可能提交不完整的代码或临时文件。

---

## 🎯 推荐工作流程

### 日常开发流程

```bash
# 1. 修改代码
# （在编辑器中编辑文件）

# 2. 查看修改
git status

# 3. 添加修改
git add .

# 4. 提交（带描述信息）
git commit -m "修复 RCD tests 验证问题"

# 5. 推送到 GitHub
git push origin main
```

### 快速提交脚本（可选）

创建一个脚本文件 `quick-commit.sh`：

```bash
#!/bin/bash
git add .
git commit -m "$1"
git push origin main
```

使用：
```bash
chmod +x quick-commit.sh
./quick-commit.sh "修复验证问题"
```

---

## ⚡ Netlify 自动部署

**好消息**：Netlify 会自动检测 GitHub 的推送并自动部署！

**工作流程**：
1. 你修改代码
2. 推送到 GitHub：`git push origin main`
3. Netlify 自动检测到推送
4. Netlify 自动构建和部署
5. 几分钟后，网站自动更新

**你不需要**：
- ❌ 手动在 Netlify 触发部署
- ❌ 每次修改都去 Netlify Dashboard

**你只需要**：
- ✅ 推送到 GitHub
- ✅ Netlify 会自动处理

---

## 🔧 简化命令（别名）

可以设置 Git 别名，简化命令：

```bash
# 设置别名
git config --global alias.save '!git add . && git commit -m'
git config --global alias.up 'push origin main'

# 使用
git save "修复问题"
git up
```

或者创建一个更简单的别名：

```bash
git config --global alias.pushall '!git add . && git commit -m "$1" && git push origin main'

# 使用
git pushall "修复验证问题"
```

---

## 📋 最佳实践

### ✅ 推荐做法

1. **频繁提交**：完成一个小功能就提交一次
2. **清晰的提交信息**：描述你做了什么
   ```bash
   git commit -m "修复 RCD tests summary 验证问题"
   git commit -m "添加调试日志"
   git commit -m "修复类型错误"
   ```
3. **推送到 GitHub**：每天结束工作前推送一次
4. **测试后再推送**：确保代码能正常运行

### ❌ 不推荐做法

1. **一次提交所有修改**：应该分多次提交，每次一个功能
2. **提交信息不清晰**：如 "fix"、"update" 等
3. **提交前不测试**：可能导致部署失败
4. **提交敏感信息**：如密码、API keys 等

---

## 🚀 快速参考

### 常用命令

```bash
# 查看状态
git status

# 添加所有修改
git add .

# 提交
git commit -m "描述信息"

# 推送
git push origin main

# 查看提交历史
git log

# 撤销最后一次提交（保留修改）
git reset --soft HEAD~1
```

### 一键推送（组合命令）

```bash
# 添加、提交、推送一步完成
git add . && git commit -m "修复问题" && git push origin main
```

---

## 💡 提示

1. **Netlify 会自动部署**：你只需要推送到 GitHub，Netlify 会自动处理
2. **可以批量提交**：不需要每个小修改都提交，可以积累几个修改后一起提交
3. **使用图形界面**：如果命令行不熟悉，使用 GitHub Desktop 或 VS Code 的 Git 界面
4. **提交前检查**：运行 `git status` 确认要提交的文件

---

## ❓ 常见问题

### Q: 可以设置自动推送吗？

**A**: 技术上可以，但不推荐。因为：
- 可能提交不完整的代码
- 可能提交临时文件或错误
- 失去对提交历史的控制

**推荐**：使用图形界面（GitHub Desktop 或 VS Code），操作简单但仍有控制。

### Q: 忘记推送会怎样？

**A**: 
- 代码只在本地，GitHub 和 Netlify 看不到
- 不会影响已部署的网站
- 下次推送时会一起上传

### Q: 推送后多久 Netlify 会部署？

**A**: 
- 通常 1-3 分钟
- 可以在 Netlify Dashboard 查看部署状态
- 部署完成后会收到通知（如果设置了）

---

## 🎯 总结

**最简单的流程**：
1. 修改代码
2. 在 GitHub Desktop 或 VS Code 中点击 "Commit" 和 "Push"
3. 等待 Netlify 自动部署（1-3 分钟）

**不需要**：
- 每次都手动输入命令
- 每次都去 Netlify Dashboard
- 设置复杂的自动化

**推荐工具**：
- GitHub Desktop（最简单）
- VS Code / Cursor 的 Git 集成（方便）
