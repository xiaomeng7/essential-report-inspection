# 部署前检查清单

## ✅ 代码准备

- [ ] 所有源代码文件都在项目中
- [ ] `package.json` 包含所有依赖
- [ ] `netlify.toml` 配置正确
- [ ] `.gitignore` 已创建（排除 node_modules, dist 等）

## ✅ 关键文件确认

确保以下文件存在：

- [ ] `netlify.toml` - Netlify 配置
- [ ] `package.json` - 依赖和脚本
- [ ] `rules.yml` - 规则文件（在项目根目录）
- [ ] `FIELD_DICTIONARY.json` - 字段定义（在项目根目录）
- [ ] `netlify/functions/submitInspection.ts` - 提交函数
- [ ] `netlify/functions/review.ts` - 查看函数
- [ ] `netlify/functions/lib/rules.ts` - 规则引擎
- [ ] `netlify/functions/lib/store.ts` - 存储模块

## ✅ 本地测试

在部署前，本地测试：

```bash
# 1. 安装依赖
npm install

# 2. 构建项目
npm run build

# 3. 检查构建输出
ls dist/          # 应该有 index.html 和静态文件
ls netlify/functions/  # 应该有函数文件

# 4. 本地运行（可选）
npm run netlify:dev
# 然后访问 http://localhost:8888 测试
```

## ✅ Git 准备

- [ ] 已初始化 Git 仓库
- [ ] 已提交所有文件
- [ ] 已推送到 GitHub/GitLab/Bitbucket

## ✅ Netlify 配置

部署时确认：

- [ ] Build command: `npm run build`
- [ ] Publish directory: `dist`
- [ ] Functions directory: `netlify/functions`
- [ ] Node version: 18+（Netlify 默认）

## ✅ 部署后验证

部署成功后测试：

1. **前端**：
   - [ ] 站点可以访问
   - [ ] 检查向导可以打开
   - [ ] 表单可以填写
   - [ ] 进度条正常显示

2. **后端 API**：
   - [ ] `POST /api/submitInspection` 可以调用
   - [ ] 返回 `inspection_id` 和 `review_url`
   - [ ] `GET /api/review/:id` 可以查看报告

3. **功能**：
   - [ ] 可以提交检查
   - [ ] 报告页面可以查看
   - [ ] Findings 正确显示
   - [ ] Limitations 正确显示

## ⚠️ 已知限制

- **存储**：当前使用内存存储，冷启动会丢失数据（v1 预期行为）
- **规则文件**：确保 `rules.yml` 在项目根目录并已提交到 Git

## 🔧 如果遇到问题

1. **查看 Netlify 构建日志**：找到错误信息
2. **检查 Functions 日志**：在 Netlify Dashboard → Functions
3. **本地测试**：运行 `npm run build` 看是否有错误
4. **查看文档**：参考 `DEPLOYMENT.md` 或 `部署指南.md`
