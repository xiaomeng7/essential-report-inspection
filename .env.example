# Google Maps API key for address autocomplete (Australian addresses only).
# Get a key at https://console.cloud.google.com/apis/credentials
# Enable: Places API, Geocoding API
GOOGLE_MAPS_API_KEY=AIzaSyC1djqNCIxsdnyP9gzuY7YNKBSWNLUU4_0

# Neon Postgres (optional). See docs/NEON_SETUP_CHECKLIST.md
# 取消下一行注释并填入从 Neon 控制台复制的连接串（需含 ?sslmode=require）
# NEON_DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# Admin API and Config Admin (optional; default token used if unset)
# ADMIN_TOKEN=your_secret_token

# ServiceM8 integration (optional)
# SERVICEM8_API_TOKEN or SERVICEM8_API_KEY: API key from ServiceM8 Settings → API Keys
# SERVICEM8_AUTH_TYPE: api_key (X-API-Key header, default) | bearer (OAuth) | basic (Basic Auth "API_KEY:"+key)
# SERVICEM8_API_BASE_URL=https://api.servicem8.com (optional)
# INTERNAL_API_KEY=your_internal_api_key (for Snapshot repo→internalServiceJobLink only; prefill has no auth)

# Data source mode for finding messages (optional)
# - "db_only"     => Never read YAML; if DB missing return clear error (production default)
# - "db_prefer"   => DB first, YAML fallback (dev default)
# - "yml_only"    => Ignore DB even if configured
# Default: "db_prefer" in dev, "db_only" in production
# DATA_SOURCE_MODE=db_prefer
