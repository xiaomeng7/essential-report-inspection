# Netlify 部署指南（中文版）

## 📋 快速步骤

### 第一步：准备代码

```bash
# 1. 进入项目目录
cd /Users/mengzhang/Downloads/essential_report_specs

# 2. 初始化 Git（如果还没有）
git init
git add .
git commit -m "准备部署到 Netlify"
```

### 第二步：推送到 GitHub

1. 在 GitHub 创建新仓库（不要初始化 README）
2. 复制仓库地址，然后运行：

```bash
git remote add origin https://github.com/你的用户名/仓库名.git
git branch -M main
git push -u origin main
```

### 第三步：在 Netlify 部署

1. **访问 Netlify**：https://www.netlify.com
2. **登录/注册**：使用 GitHub 账号登录（推荐）
3. **创建站点**：
   - 点击 "Add new site" → "Import an existing project"
   - 选择 GitHub
   - 授权并选择你的仓库
   - 点击 "Import"
4. **配置（通常自动检测）**：
   - Build command: `npm run build`
   - Publish directory: `dist`
   - Functions directory: `netlify/functions`
5. **点击 "Deploy site"**
6. **等待构建完成**（1-3 分钟）

### 第四步：测试

构建成功后，Netlify 会提供一个链接，如：
`https://your-site-123.netlify.app`

打开链接测试：
- ✅ 检查向导可以打开
- ✅ 可以填写并提交检查
- ✅ 报告页面可以查看

---

## ⚙️ 配置说明

### netlify.toml（已配置好）

```toml
[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"
```

### 环境变量

**当前不需要环境变量**。如果将来需要，在 Netlify Dashboard：
**Site settings** → **Environment variables** 添加

---

## 🔧 常见问题

### ❌ 构建失败

**检查**：
1. 查看 Netlify 构建日志中的错误信息
2. 确认 `package.json` 包含所有依赖
3. 本地运行 `npm run build` 看是否有错误

### ❌ API 404 错误

**检查**：
1. 确认 `netlify/functions/` 目录存在
2. 确认 `netlify.toml` 中的 redirects 配置正确

### ❌ 页面刷新 404

**已解决**：`netlify.toml` 中已配置 `from = "/*" to = "/index.html"`

---

## 📝 部署后检查清单

- [ ] 站点可以访问
- [ ] 检查向导正常显示
- [ ] 可以填写表单
- [ ] 提交检查成功
- [ ] 报告页面可以查看
- [ ] Functions 日志无错误

---

## 🚀 自动部署

Netlify 会自动：
- ✅ 每次推送到 `main` 分支 → 自动部署
- ✅ 推送到其他分支 → 创建预览部署

---

## 📞 需要帮助？

查看详细文档：`DEPLOYMENT.md`（英文版）
